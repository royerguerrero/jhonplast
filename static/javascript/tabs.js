const tabs = document.querySelectorAll('.tabs')

tabs.forEach(tab => {
    const tabItems = tabs.querySelectorAll('.tabs__items')
    const tabParagraphs = tabs.querySelectorAll('.tabs__paragraph')
})